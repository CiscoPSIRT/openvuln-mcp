[project]
name = "openvuln-mcp"
version = "0.1.0"
description = "MCP Server for Cisco OpenVuln API"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv~=1.2.1",
    "fastmcp~=2.14.3",
    "httpx~=0.28.1",
    "pyjson5~=2.0.0",
]
